env = Environment(CFLAGS='-g')
env.Library('proclib', ['proclib.c'])
env.Program('prog_proclist', 'proclist.c', LIBS=['proclib'], LIBPATH='.')
env.Program('prog_pstree', 'pstree.c', LIBS=['proclib'], LIBPATH='.')
env.Program('prog_sherlock', 'sherlock.c', LIBS=['proclib'], LIBPATH='.')
